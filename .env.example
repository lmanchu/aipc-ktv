# YouTube Data API v3 Configuration

# Required: YouTube Data API v3 API Key
# Get your API key from: https://console.cloud.google.com/apis/credentials
#
# Steps:
# 1. Create a Google Cloud project (if you don't have one)
# 2. Enable the YouTube Data API v3
# 3. Create an API key (Credentials → Create Credentials → API Key)
# 4. Restrict the API key to YouTube Data API v3 only (recommended)
# 5. Copy the API key below

VITE_YOUTUBE_API_KEY=your_youtube_api_key_here

# Note: This file is for documentation only. Copy it to .env and add your actual API key.
# The .env file is git-ignored and will not be committed to version control.
